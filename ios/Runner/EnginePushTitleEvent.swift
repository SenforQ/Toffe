
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let loggerDocumentPath:String = "fail bodygetDe"
fileprivate let helperNativeAcrossKey:[Character] = ["v","i","c","e","I","D"]

/*: "getFirebaseID" :*/
fileprivate let main_padMsg:[Character] = ["g","e","t","F","i","r","e","b"]
fileprivate let main_okValue:String = "aseIDafter cancel"

/*: "getAreaISO" :*/
fileprivate let userExhibitDate:[Character] = ["g","e","t","A","r","e","a","I"]
fileprivate let sunProtectionFactorVersion:[Character] = ["S","O"]

/*: "getProxyStatus" :*/
fileprivate let loggerScaleCurrentToken:String = "remove m importgetPro"
fileprivate let mainWarnVersion:String = "accept trigger pad waitxyStatus"

/*: "getMicStatus" :*/
fileprivate let constEspionageNetworkCount:String = "reduce transaction height soundgetMi"
fileprivate let routerMakePath:[Character] = ["s"]

/*: "getPhotoStatus" :*/
fileprivate let app_againToken:[Character] = ["g","e","t","P","h","o","t","o","S","t","a"]
fileprivate let enlivenerTingResult:String = "messages"

/*: "getCameraStatus" :*/
fileprivate let show_kitStateID:String = "getCview media clear"
fileprivate let modelOrderedSeriesContactKey:[Character] = ["t","a","t","u","s"]

/*: "reportAdjust" :*/
fileprivate let data_ratingPolicyImageToken:[Character] = ["r","e","p","o","r"]
fileprivate let loggerKindUrl:String = "tAdjustblock of generate background tool"

/*: "requestLocalPush" :*/
fileprivate let appReWhiteErrorToken:[Character] = ["r","e","q","u","e","s","t","L"]
fileprivate let notiOptionPadValue:String = "global sharedocalPush"

/*: "getLangCode" :*/
fileprivate let viewAcrossFatalPath:String = "kit"
fileprivate let networkNoKey:String = "on close request in linktLan"

/*: "getTimeZone" :*/
fileprivate let main_responseError:[Character] = ["g","e","t","T","i","m","e","Z","o","n","e"]

/*: "getInstalledApps" :*/
fileprivate let routerRollSecret:String = "reduce response please of gogetIn"
fileprivate let constBlockError:String = "ledApfeedback substance"
fileprivate let helperServerKey:String = "adjusts"

/*: "getSystemUUID" :*/
fileprivate let captureRecordValue:String = "between track register rungetSys"

/*: "getCountryCode" :*/
fileprivate let serviceContactSoundValue:String = "getCoustate app presentation model"

/*: "inAppRating" :*/
fileprivate let viewFilterFlag:[Character] = ["i","n","A","p","p","R"]
fileprivate let configInsertLabID:[Character] = ["a","t","i","n","g"]

/*: "apPay" :*/
fileprivate let formatterCenterDataAcceptTitle:String = "show or styleapPay"

/*: "subscribe" :*/
fileprivate let loggerLanguageVersionFormat:String = "appearbsc"

/*: "openSystemBrowser" :*/
fileprivate let app_originTakeDate:String = "model autoopenSy"
fileprivate let noti_serverError:String = "fieldwse"
fileprivate let appFeedbackMessage:String = "observer"

/*: "closeWebview" :*/
fileprivate let formatterVisibleFromFlag:String = "closeWescript status pad except written"
fileprivate let userTempRegionFormat:String = "BVIEW"

/*: "openNewWebview" :*/
fileprivate let const_promptBodyString:String = "openNmodel height"
fileprivate let k_endMainFormat:[Character] = ["e","w","W","e","b","v","i","e","w"]

/*: "reloadWebview" :*/
fileprivate let const_labelCheckedData:String = "relnowa"
fileprivate let parserOffPermissionList:String = "tun instancedWebview"

/*: "typeName" :*/
fileprivate let factoryEnterCount:[UInt8] = [0x5b,0x56,0x5f,0x4a,0x61,0x4e,0x42,0x4a]

/*: "deviceID" :*/
fileprivate let mainAcceptEndDict:[UInt8] = [0xfa,0xfb,0xc,0xff,0xf9,0xfb,0xdf,0xda]

fileprivate func sumHead(handle num: UInt8) -> UInt8 {
    let value = Int(num) - 150
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fireBaseID" :*/
fileprivate let const_transactionTime:[UInt8] = [0x94,0x9b,0x80,0x97,0xb0,0x93,0x81,0x97,0xbb,0xb6]

private func nameActiveExhibit(request num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "areaISO" :*/
fileprivate let parserPlainspokenTotalSecret:[UInt8] = [0x96,0xa7,0x9a,0x96,0x7e,0x88,0x84]

fileprivate func textConfirm(label num: UInt8) -> UInt8 {
    let value = Int(num) + 203
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isProxy" :*/
fileprivate let deadlineSourceUrl:[UInt8] = [0x97,0xa1,0x7e,0xa0,0x9d,0xa6,0xa7]

fileprivate func successTun(shared num: UInt8) -> UInt8 {
    let value = Int(num) - 46
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "langCode" :*/
fileprivate let showWoodList:[UInt8] = [0x19,0x14,0x1b,0x12,0x36,0x1a,0x11,0x10]

/*: "timeZone" :*/
fileprivate let noti_rootMsg:[UInt8] = [0x78,0x65,0x61,0x69,0x56,0x63,0x62,0x69]

private func numberMain(control num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "installedApps" :*/
fileprivate let factoryThatTotalervalTitle:[UInt8] = [0xc2,0xc7,0xcc,0xcd,0xba,0xc5,0xc5,0xbe,0xbd,0x9a,0xc9,0xc9,0xcc]

fileprivate func clickTemp(script num: UInt8) -> UInt8 {
    let value = Int(num) - 89
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "systemUUID" :*/
fileprivate let user_modelID:[UInt8] = [0xea,0xf0,0xea,0xeb,0xdc,0xe4,0xcc,0xcc,0xc0,0xbb]

fileprivate func atomicQuantity85Foundation(time num: UInt8) -> UInt8 {
    let value = Int(num) - 119
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "countryCode" :*/
fileprivate let k_styleClearURL:[UInt8] = [0xbb,0xc7,0xcd,0xc6,0xcc,0xca,0xd1,0x9b,0xc7,0xbc,0xbd]

fileprivate func subCore(past num: UInt8) -> UInt8 {
    let value = Int(num) + 168
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "status" :*/
fileprivate let showRevenueData:[UInt8] = [0x65,0x62,0x77,0x62,0x63,0x65]

private func cameraTimeActive(close num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "isAuth" :*/
fileprivate let loggerFactoryAlwaysTitle:[UInt8] = [0x68,0x74,0x75,0x41,0x73,0x69]

/*: "isFirst" :*/
fileprivate let controllerZoneError:[UInt8] = [0x67,0x71,0x44,0x67,0x70,0x71,0x72]

fileprivate func fullScript(data num: UInt8) -> UInt8 {
    let value = Int(num) + 2
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let networkSubstanceBehaviorMessage:String = "body user post prompt us__LocalP"
fileprivate let main_eventStatus:String = "uspic"

/*: "identifier" :*/
fileprivate let dataNumericalQuantityPath:[UInt8] = [0x86,0x8b,0x8a,0x81,0x9b,0x86,0x89,0x86,0x8a,0x9d]

private func imageAlbumProduct(time num: UInt8) -> UInt8 {
    return num ^ 239
}

/*: "HTTPProxy" :*/
fileprivate let dataObjectMsg:[Character] = ["H","T","T","P","P","r","o","x","y"]

/*: "HTTPSProxy" :*/
fileprivate let helperProtectionPath:[Character] = ["H","T","T","P","S","P"]
fileprivate let k_atHandID:String = "rkindy"

/*: "SOCKSProxy" :*/
fileprivate let configFailState:[Character] = ["S","O","C","K","S","P","r"]
fileprivate let controllerCompoundLensValue:String = "oxtrust"

/*: "__SCOPED__" :*/
fileprivate let k_netSecret:String = "__SCOmirror receive"

/*: "tap" :*/
fileprivate let sessionKindValue:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let constDecisionCount:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let kCarrierLaunchDict:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let k_onName:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let showPhotoSizeId:[Character] = ["R","e","t","r","y"," ","A","f","t","e","r"," ","o","r"]
fileprivate let notiTimeNoStr:String = "open take object float Go to "

/*: "Feedback" :*/
fileprivate let serviceArrowTime:String = "Feedwill image core"

/*: " to contact us" :*/
fileprivate let appPatronymicSecret:[Character] = [" ","t","o"," ","c","o","n","t","a","c","t"," ","u"]
fileprivate let main_fireCaptureID:[Character] = ["s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let parserAdAppMicSecret = (String(loggerDocumentPath.suffix(5)) + String(helperNativeAcrossKey)) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let user_acceptFormat = (String(main_padMsg) + String(main_okValue.prefix(5))) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let dataRetrofitKey = (String(userExhibitDate) + String(sunProtectionFactorVersion)) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let notiMicAcceptableVersion = (String(loggerScaleCurrentToken.suffix(6)) + String(mainWarnVersion.suffix(8))) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let dataAppSecret = (String(constEspionageNetworkCount.suffix(5)) + "cStatu" + String(routerMakePath)) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let configPageTitle = (String(app_againToken) + enlivenerTingResult.replacingOccurrences(of: "message", with: "tu")) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let serviceStatusDate = (String(show_kitStateID.prefix(4)) + "ameraS" + String(modelOrderedSeriesContactKey)) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let domainCameraData = (String(data_ratingPolicyImageToken) + String(loggerKindUrl.prefix(7))) // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let cacheExecuteList = (String(appReWhiteErrorToken) + String(notiOptionPadValue.suffix(8))) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let app_keyKey = (viewAcrossFatalPath.replacingOccurrences(of: "kit", with: "ge") + String(networkNoKey.suffix(4)) + "gCode") // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let enginePushTitle = (String(main_responseError)) // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let const_unobjectionableId = (String(routerRollSecret.suffix(5)) + "stal" + String(constBlockError.prefix(5)) + helperServerKey.replacingOccurrences(of: "adjust", with: "p")) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let appExecuteStatusTime = (String(captureRecordValue.suffix(6)) + "temUUID") // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let k_numberServerResult = (String(serviceContactSoundValue.prefix(6)) + "ntryCode") // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let helperDismissFactoryValue = (String(viewFilterFlag) + String(configInsertLabID)) // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let serviceAppGenerateMessage = (String(formatterCenterDataAcceptTitle.suffix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let createHeapVersion = (loggerLanguageVersionFormat.replacingOccurrences(of: "appear", with: "su") + "ribe") // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let userDeviceExecuteStr = (String(app_originTakeDate.suffix(6)) + "stemBr" + noti_serverError.replacingOccurrences(of: "field", with: "o") + appFeedbackMessage.replacingOccurrences(of: "observer", with: "r")) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let appRatingMode = (String(formatterVisibleFromFlag.prefix(7)) + userTempRegionFormat.lowercased()) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let engineBlockNeedString = (String(const_promptBodyString.prefix(5)) + String(k_endMainFormat)) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let show_buildPath = (const_labelCheckedData.replacingOccurrences(of: "now", with: "o") + String(parserOffPermissionList.suffix(8))) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct SymboliseChromosphereCapacity: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: ForeignRun?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct ForeignRun: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension AlbumManager {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func execute(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = SymboliseChromosphereCapacity.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case parserAdAppMicSecret:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = GlobalEqual.show()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: mainAcceptEndDict.map{sumHead(handle: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case user_acceptFormat:
                //: AppWebViewController.getFireBaseToken { str in
                AlbumManager.disappear { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: const_transactionTime.map{nameActiveExhibit(request: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case dataRetrofitKey:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = AlbumManager.showName()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: parserPlainspokenTotalSecret.map{textConfirm(label: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case notiMicAcceptableVersion:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = AlbumManager.handleZone()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: deadlineSourceUrl.map{successTun(shared: $0)}, encoding: .utf8)!: status])

            //: case getLangCode:
            case app_keyKey:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: showWoodList.map{$0^117}, encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case enginePushTitle:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: noti_rootMsg.map{numberMain(control: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case const_unobjectionableId:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: factoryThatTotalervalTitle.map{clickTemp(script: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case appExecuteStatusTime:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: user_modelID.map{atomicQuantity85Foundation(time: $0)}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case k_numberServerResult:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: k_styleClearURL.map{subCore(past: $0)}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case helperDismissFactoryValue:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                AlbumManager.allClear()

            //: case apPay:
            case serviceAppGenerateMessage:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.capture(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: showRevenueData.map{cameraTimeActive(close: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case createHeapVersion:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.capture(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: showRevenueData.map{cameraTimeActive(close: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case userDeviceExecuteStr:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case appRatingMode:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.load()

            //: case openNewWebview:
            case engineBlockNeedString:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    AlbumManager.oneTime(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case show_buildPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.component()

            //: case getMicStatus:
            case dataAppSecret:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                OverdoPing.shared.writtenRecordMagnitude { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: loggerFactoryAlwaysTitle.reversed(), encoding: .utf8)!: auth, String(bytes: controllerZoneError.map{fullScript(data: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case configPageTitle:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                OverdoPing.shared.make { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: loggerFactoryAlwaysTitle.reversed(), encoding: .utf8)!: auth, String(bytes: controllerZoneError.map{fullScript(data: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case serviceStatusDate:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                OverdoPing.shared.willProduct { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName, String(bytes: loggerFactoryAlwaysTitle.reversed(), encoding: .utf8)!: auth, String(bytes: controllerZoneError.map{fullScript(data: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case domainCameraData:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        GlobalEqual.indicatorWith(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        GlobalEqual.confirm(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case cacheExecuteList:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: factoryEnterCount.map{$0^47}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                AlbumManager.time(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension AlbumManager {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func oneTime(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = AlbumManager()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        SteamrollInstallation.restaurantAttendant()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func time(_ model: SymboliseChromosphereCapacity) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(show_saveString)" + (String(networkSubstanceBehaviorMessage.suffix(8)) + main_eventStatus.replacingOccurrences(of: "pic", with: "h"))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: dataNumericalQuantityPath.map{imageAlbumProduct(time: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func disappear(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func showName() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func handleZone() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if AlbumManager.utilisedValue() || AlbumManager.fromPost() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func utilisedValue() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(dataObjectMsg))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(helperProtectionPath) + k_atHandID.replacingOccurrences(of: "kind", with: "ox"))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(configFailState) + controllerCompoundLensValue.replacingOccurrences(of: "trust", with: "y"))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func fromPost() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(k_netSecret.prefix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(sessionKindValue))) || keyStr.contains((String(constDecisionCount))) || keyStr.contains((String(kCarrierLaunchDict))) || keyStr.contains((String(k_onName))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func allClear() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func capture(productId: String, source: Int = -1, payType: DoingrushFlexible, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        WheelbarrowForce.nowInfo()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        SenateCampaignQuantityerval.shared.run(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            WheelbarrowForce.permission()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    WheelbarrowForce.inputAcross((String(showPhotoSizeId) + String(notiTimeNoStr.suffix(7))) + "\"" + (String(serviceArrowTime.prefix(4)) + "back") + "\"" + (String(appPatronymicSecret) + String(main_fireCaptureID)))

                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.path()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
